<h2><%= @post.title %></h2>
<%= @post.content %>


<h5>New Comment:</h5>
<%= form_for [@post, @comment] do |f| %>
  <p><%= f.text_area :content, placeholder: "Write something..." %></p>
  <%= f.submit "Post" %>
<% end %>

<% @post.comments.each do |c| %>
  <p><%= c.content %></p>
<% end %>
